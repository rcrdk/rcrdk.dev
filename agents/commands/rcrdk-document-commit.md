---
name: rcrdk-document-commit
description: Focused docs plus a conventional commit message (doc + git mental model)
---

In the **same response**:

1. **`document-specialist`** — update only what was asked (README, JSDoc, deploy notes); do not invent unsolicited documentation.
2. **`git-master`** — propose **one** Conventional Commits message plus the file list that would go in the commit.

**Do not** run `git commit` or `push` unless the user explicitly asks.

---
description: Bootstrap Superpowers — invoke relevant skills from agents/skills/ before any response or action
alwaysApply: true
---

# Using Superpowers

Source: [obra/superpowers](https://github.com/obra/superpowers) v6.0.3

## Priority

1. User instructions (`AGENTS.md`, direct requests) — highest
2. Superpowers skills in `agents/skills/` — override default behavior
3. Default system prompt — lowest

If user says "skip TDD" and a skill says "always TDD", follow the user.

## The Rule

**Before any response or action**, check if a skill in `agents/skills/` applies — even 1% chance means read and follow it.

- Use Cursor's skill mechanism to load skills (do not skim SKILL.md without activating)
- Process skills first (brainstorming, systematic-debugging), then implementation skills
- Rigid skills (TDD, systematic-debugging): follow exactly
- Subagent dispatched for a specific task: skip `using-superpowers`

## Workflow

1. **brainstorming** — before creative work (features, components, behavior changes)
2. **using-git-worktrees** — isolated branch after design approval
3. **writing-plans** — bite-sized tasks with file paths and verification steps
4. **subagent-driven-development** or **executing-plans** — execute plan with review
5. **test-driven-development** — RED-GREEN-REFACTOR during implementation
6. **requesting-code-review** — between tasks
7. **finishing-a-development-branch** — merge/PR/cleanup when done

Debugging: **systematic-debugging** → **verification-before-completion**

Full index: [agents/README.md](../README.md#superpowers)
